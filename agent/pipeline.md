工作流程：
1. 导入收货清单。获取入库数据后，分发补充至客户文件内（客户文件具体数据格式请查看 `/agent/format/` 下文件）,格式需要模仿已有格式。
2. 用户/管理员手动组柜生成清单（Draft）
3. 货柜装货经过工作人员检查后，出仓（POSTED），此时一键更新各客户信息，在文件后加上“柜次出仓/运费结算标记行”（具体数据格式也请查看`/agent/format/` 下文件），代表一次出库结算


补充：目前资金结算仍由手动完成，数据库系统不需介入太多。
