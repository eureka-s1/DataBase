# Update Log

### 1. 新增数据库设计文档
- 时间：2026-04-04
- 更新内容：创建 `agent/database_design.md`，完成需求整合、原始数据采样、完整数据库设计、技术栈、开发流程、可扩展性分析。
- 更新原因：落实 `agent/AGENTS.md` 指定的设计产出要求。

### 2. 设计文档补充（别名 + 环境）
- 时间：2026-04-04
- 更新内容：
  - 新增“客户别名映射”需求与表结构 `customer_aliases`。
  - 新增“WSL 开发 -> Windows 运行”环境约束与发布要求。
  - 在建库 SQL 与示例 SQL 中加入别名映射相关语句。
- 更新原因：响应最新业务要求，降低历史多命名导致的客户识别错误，并确保跨环境可部署。

### 3. 第一阶段代码实现（MVP 基础）
- 时间：2026-04-04
- 更新内容：
  - 新增数据库脚本：`schema/schema.sql`。
  - 新增应用代码：`app/`（配置、数据库连接、客户与别名服务、Flask 接口）。
  - 新增脚本：`scripts/init_db.py`、`scripts/import_alias_map.py`。
  - 新增跨环境文件：`.env.example`、`start_windows.bat`、`requirements.txt`。
  - 更新 `README.md` 说明启动方式和接口。
- 更新原因：进入开发实施阶段，先建立可运行基线并打通客户别名能力。

### 4. 第二阶段实现（覆盖核心业务需求）
- 时间：2026-04-04
- 更新内容：
  - 新增认证与账户能力：默认管理员、登录、改密、退出会话。
  - 完成入库业务：入库增删改查、体积自动计算、在库查询。
  - 完成拼柜业务：建柜、装柜容量校验、确认出柜、撤回恢复。
  - 完成结算业务：定金流水、按客户体积分摊运费、自动抵扣、账单过账。
  - 完成报表导出：日入库、库存、总账、账单（Excel/PDF）。
  - 完成备份能力：SQLite 备份接口与脚本，写入备份日志。
  - 完成 Excel 导入链路：模板识别、字段映射、预览与执行接口，支持 dry-run。
  - 新增基础 Web 页面：登录页、首页、业务面板。
  - 新增端到端冒烟脚本 `scripts/smoke_test.py`，覆盖登录→入库→拼柜→结算→导出→备份→导入预演。
- 更新原因：根据“直接做到完成所有需求，停在导入数据前”为目标，交付可运行版本并将真实数据导入保留为下一步人工确认操作。

### 5. 新增项目打包分发脚本（Windows/跨平台）
- 时间：2026-04-04
- 更新内容：
  - 新增打包脚本：`scripts/package_release.py`（生成 `dist/*.zip` 与 `*.sha256`）。
  - 新增 Windows 一键打包入口：`package_release.bat`。
  - 更新 `README.md`：新增“非技术用户一键启动”和“一键打包（发给同事）”说明。
- 更新原因：支持在 Windows 测试前快速生成发布包，并便于直接分发给业务同事使用。

### 6. 打包引导文案本地化（中文）
- 时间：2026-04-04
- 更新内容：
  - 将发布包内 `WINDOWS_QUICK_START.txt` 改为中文说明。
  - 强化“首次启动等待、不要关闭命令窗口、默认数据目录、常见问题”等引导细节。
  - 使用 `UTF-8-SIG` 写入，提升 Windows 记事本兼容性，减少乱码风险。
- 更新原因：提升非技术用户的可读性与可操作性，降低分发后使用门槛。

### 7. Web 面板重构与功能补齐
- 时间：2026-04-04
- 更新内容：
  - 重构 `app/templates/dashboard.html`，改为“模块切换式”布局，避免全部表格并排挤在同一页。
  - 新增网页可操作能力：修改密码、在库查询、总账查询、客户创建、别名维护（折叠显示）、价格规则维护。
  - 新增拼柜操作表单：建柜、装柜、移除、占用查询、确认、撤回。
  - 新增结算操作表单：登记定金、生成结算单、结算单过账。
  - 新增导入导出与备份表单：导入预览/执行、日入库/库存/总账/账单导出、数据库备份。
  - 新增后端接口 `GET /settlements` 用于前端查询结算单列表。
- 更新原因：响应“网页端功能缺失、查询能力不足、信息过于拥挤”的反馈，提升可用性与操作闭环。

### 8. 导入测试 Loop 与导入器迭代
- 时间：2026-04-04
- 更新内容：
  - 新增导入规范文档：`agent/import_data.md`。
  - 新增导入执行模板：`agent/import_run_template.md`。
  - 新增导入体检与报告：`agent/import_loop_report.md`。
  - 新增脚本：`scripts/bootstrap_customers_from_data.py`（按 `2026data` 目录预置客户与别名）。
  - 新增脚本：`scripts/run_import_loop.py`（批量执行 `preview -> dry-run -> commit` 并生成报告）。
  - 迭代 `app/services/importer.py`：
    - 扩展真实模板表头映射（含 `ITEM NAME/CNS/DEPOZIT` 等变体）。
    - 支持无表头“裸数据”模板解析与文件夹客户名回退。
    - 修复 `import_batches.batch_no` 唯一键冲突（批次号增加随机后缀）。
    - 修复 `inbound_no` 跨文件重复（单号引入 `batch_id` 维度）。
    - 增加非货物行过滤（如 `CBM FREIGHT/SEND TO ME/100CTNS` 等）。
  - 更新原因：支撑“清库后立即进入导入+测试循环”，先确保样本批量可导，再逐步扩大导入规模。

### 9. 导入 Loop 扩大到全量候选文件
- 时间：2026-04-04
- 更新内容：
  - 执行第 3 轮：`limit=600, commit-files=60`，达成 `parse_fail=0`、`dry_run failed_rows=0`。
  - 执行第 4 轮（全量）：`limit=0, commit-files=100`。
  - 全量结果：候选文件 `612`，解析成功 `611`，解析失败 `1`，`dry-run failed_rows=0`，正式导入 `100` 个文件。
  - 修复多级目录客户回退策略：默认客户名优先取 `2026data` 下一级目录，避免 `2025 11/2025 12` 等子目录名误映射。
- 更新原因：将导入能力从小批样本推进到全量候选验证，并识别剩余极少量异常文件供后续专项处理。

### 10. 全量 612/612 导入测试闭环
- 时间：2026-04-04
- 更新内容：
  - 针对专项失败文件 `2026data/胡订货/2-18 EKENE .1.xlsx` 追加订货单模板兼容：
    - 增加表头映射 `DESCRIPTION/DESCRPETION`。
    - 表头检测允许 `item_no` 作为可导入主字段（无 `item_name_cn` 时回退）。
  - 复跑全量 loop（`limit=0, commit-files=100`）结果：
    - 候选文件 `612`，解析成功 `612`，解析失败 `0`。
    - `dry_run`：总行数 `29801`，成功 `29801`，失败 `0`。
    - 正式导入文件数 `100`。
- 更新原因：完成全量导入测试主目标，形成可持续复用的导入验证闭环。

### 11. 收货清单导入重构（字段不丢失）
- 时间：2026-04-04
- 更新内容：
  - 重构 `app/services/importer.py`，导入链路改为面向“收货清单”文件：
    - 仅允许导入文件名包含“收货清单”的 Excel。
    - 增强表头映射，支持 `DESCRIPTION/DESCRPETION/T.T/DEP` 等字段。
    - 支持无表头兜底解析，并保留每行原始字段。
  - 新增表 `inbound_import_rows`（见 `schema/schema.sql`）用于逐行保存：
    - 原始行 JSON（`source_row_json`）
    - 规范化行 JSON（`normalized_row_json`）
    - 行有效性与错误原因
    - 关联 `import_batch_id / inbound_item_id`
  - 调整 `scripts/run_import_loop.py`：候选文件仅筛选“收货清单”。
  - 调整 `scripts/smoke_test.py`：导入样例文件名改为包含“收货清单”。
- 更新原因：解决“收货清单字段被省略”的核心问题，确保导入后可追溯原始条目，不丢字段。

### 12. 网页端手动上传收货清单
- 时间：2026-04-04
- 更新内容：
  - 新增接口 `POST /import/inbound/upload`，支持网页端上传 `.xls/.xlsx`。
  - 上传校验：仅允许文件名包含“收货清单”。
  - 新增上传目录配置 `CANYU_IMPORT_UPLOAD_DIR`（默认 `.canyu_data/imports`）。
  - 更新业务面板“导入导出”模块：支持本地选文件上传，自动回填 `file_path` 后执行预览/导入。
- 更新原因：实现用户要求的网页端手动导入收货清单流程，替代手工填写服务器路径。

### 13. 导入交互优化与批次撤销
- 时间：2026-04-04
- 更新内容：
  - 新增导入批次撤销能力：
    - 后端接口 `POST /import/inbound/rollback/<batch_id>`。
    - 仅允许撤销该批次中仍为 `IN_STOCK` 的记录，避免影响已装柜/已出柜业务数据。
    - 撤销时同步删除 `inbound_import_rows` 与对应 `inbound_items`。
  - 优化导入区交互（非技术用户友好）：
    - 预览结果改为可关闭面板（不再是难读 JSON 常驻显示）。
    - 预览内容改为中文摘要 + 样例表格。
    - 导入完成增加弹窗提示（批次号、成功条数、失败条数）。
    - 导入后自动刷新当日入库列表，便于立即核对是否写入数据库。
    - “仅演练”默认值改为“否（默认直接导入）”。
  - 接口默认值调整：
    - `POST /import/inbound/execute` 未传 `dry_run` 时默认按正式导入处理。
- 更新原因：解决“预览难用、导入结果不直观、缺少撤销、默认行为与业务预期不符”等问题，提升验收可用性。

### 14. 导入自动建客户 + 客户合并 + 日期自动提取
- 时间：2026-04-04
- 更新内容：
  - 导入时客户匹配增强：
    - 若无法通过别名映射到现有客户，自动新建客户档案并建立别名映射。
    - 导入返回中新增 `auto_created_customer_count` 与 `auto_created_customers`，用于回显本批自动建档情况。
  - 新增客户合并能力：
    - 后端接口 `POST /customers/merge`，支持将来源客户并入目标客户。
    - 合并时同步迁移别名、入库记录、付款记录、价格规则、结算明细（含冲突合并）。
    - 来源客户自动置为 `is_active=0`，后续通过任一别名仍可匹配到目标客户。
  - 导入日期自动提取：
    - `inbound_date` 允许为空，系统从文件名/路径自动提取日期（如 `2025-03-18 收货清单.xlsx`、`11月21日收货清单`）。
    - 导入返回中新增 `inbound_date_used`。
  - 前端导入区同步更新：
    - 入库日期改为可选输入（可空自动提取）。
    - 新增“客户合并”操作入口。
- 更新原因：解决“导入全部失败因无对应用户”与“导入日期需自动识别”的核心业务痛点，降低人工维护成本。

### 15. 手动添加客户别名能力增强
- 时间：2026-04-04
- 更新内容：
  - 别名接口 `POST /customer-aliases` 支持两种定位方式：
    - 通过 `customer_id` 添加别名；
    - 通过 `customer_name` 添加别名（自动解析客户）。
  - Web 页面“别名维护”改为可填“客户ID或客户名（二选一）”，降低操作门槛。
- 更新原因：补齐手动维护别名场景，减少非技术用户必须先查 ID 的操作负担。

### 16. 查询与导入体验修正（字段/分页/日期）
- 时间：2026-04-04
- 更新内容：
  - 在“查询”页入库列表中展示收货清单核心字段：
    - `CUSTOMER NAME, SHOP NO, 位置, ITEM NO, 品名, 材质, CTNS, QTY, PRICE, T.PRICE, 定金, CBM`
  - 查询页新增分页能力：
    - 可选择每页条数（20/50/100/200）
    - 支持上一页/下一页
    - 显示总条数与当前页码
  - 总账查询同步加入分页能力（可选每页条数 + 翻页）。
  - 日期自动提取增强：
    - 当文件名仅有“月日”无年份时，默认使用系统当前年份（如 `11月21日收货清单` -> `2026-11-21`）。
- 更新原因：响应“字段显示不全、查询滚动成本高、日期提取失败”的验收反馈，提升业务可读性与操作效率。

### 17. 日期填充与别名/总账显示修正
- 时间：2026-04-04
- 更新内容：
  - 导入日期策略调整为“不再自动提取文件名日期”：
    - `inbound_date` 为空时后端默认系统当天日期；
    - 页面提供“一键填充今天”按钮，便于人工确认后再导入。
  - 客户与总账可见性优化：
    - 客户列表由“别名数”改为直接显示“别名列表”；
    - 总账表保留“别名”列，便于核对合并结果。
  - 合并/别名维护后的页面同步刷新：
    - 执行客户合并、保存别名后，自动刷新客户列表与总账数据，避免旧缓存造成“总账未同步”错觉。
  - 查询字段补齐：
    - 在库查询表恢复展示 `状态` 字段。
- 更新原因：统一日期操作习惯（显式填充系统日期），并提升客户合并后的核对效率与结果可见性。

### 18. 在库表字段补齐与 SHOP NO 缺省补全
- 时间：2026-04-04
- 更新内容：
  - 在库查询表新增“入库日期”列，便于按天核对导入结果。
  - 收货清单导入解析增强：`SHOP NO` 支持缺省补全。
    - 当同一客户后续行未重复填写 `SHOP NO` 时，自动沿用该客户最近一次有效 `SHOP NO`。
    - 适配表头模板与无表头兜底模板两条解析路径。
- 更新原因：解决“字段展示不全”和“`SHOP NO` 经常只写一行导致后续空值”的验收问题。

### 19. 总览实时刷新 + 批次查询撤销 + 单条手动撤销
- 时间：2026-04-04
- 更新内容：
  - 新增总览统计接口：`GET /dashboard/summary`。
    - 统计活跃客户数、在库货物数、柜次数、结算单数。
    - 页面在导入/撤销/合并及其他关键写操作后自动刷新总览，避免首页数据滞后。
  - 在库查询增强：
    - 支持按 `batch_id` 查询（`GET /inbound-items?batch_id=...`）。
    - 表格新增 `ID`、`批次ID`、`操作` 列，可对单条在库货物执行“撤销”（调用 `DELETE /inbound-items/<id>`）。
  - 批次查询与统一撤销：
    - 新增接口 `GET /import/inbound/batches`（支持按批次ID、入库日期、最近条数查询）。
    - 导入页面新增“批次查询表”，每条批次可直接“撤销此批”。
- 更新原因：解决“导入后首页总览不及时”和“撤销缺少批次可视化入口、无法单条撤销”的验收问题。

### 20. 单条撤销外键修复 + 在库商品手动修改
- 时间：2026-04-04
- 更新内容：
  - 修复单条撤销报错 `sqlite3.IntegrityError: FOREIGN KEY constraint failed`：
    - 删除在库商品前，先将 `inbound_import_rows.inbound_item_id` 置空，再删除 `inbound_items`；
    - 兼容清理异常残留的 `container_items` 引用，避免历史脏数据触发外键失败。
  - 查询页新增“手动修改在库商品”表单：
    - 支持按 `货物ID` 修改入库日期、店铺号、位置、货号、品名、材质、数量、价格、定金、CBM覆盖、状态等字段；
    - 在库表新增“编辑”按钮，可一键带入当前行数据到编辑表单并保存。
- 更新原因：解决单条撤销不可用问题，并补齐在库数据的人工修正入口。

### 21. 拼柜工作台重构（未出柜货池/实时占用/体积调整/出柜撤回）
- 时间：2026-04-04
- 更新内容：
  - 拼柜页面新增“拼柜工作台”：
    - 展示所有未出柜货物（过滤 `status != SHIPPED`），支持按“客户名/入库日期”排序和关键字筛选；
    - 通过勾选批量加入当前柜次（替代手工输入货物 ID）。
  - 实时容量显示：
    - 选中柜次后实时显示 `已用CBM / 容量CBM / 剩余CBM`，并在加入/移除/修改体积后自动刷新。
  - 柜内货物体积可编辑：
    - 新增接口 `PUT /containers/<container_id>/items/<item_id>`，支持修改 `cbm_at_load`；
    - 页面在“当前柜内货物”表支持直接修改并保存。
  - 柜内明细接口：
    - 新增 `GET /containers/<container_id>/items`，用于展示当前柜次的已选货物与装柜体积。
  - 业务状态流对齐需求：
    - “完成组柜”后，柜内货物状态改为 `SHIPPED`（已出柜，库存查询默认不再显示）；
    - “撤回柜次”后，货物状态恢复 `IN_STOCK`，并清空柜次关联，支持重新拼柜。
- 更新原因：落实 customer requirements 中“找货-拼柜-防超载-体积修正-出柜-撤回”的核心闭环。

### 22. 拼柜体验细化（隐藏ALLOCATED/分页/反馈增强/默认6100）
- 时间：2026-04-04
- 更新内容：
  - 未出柜货物池新增“隐藏已分配(ALLOCATED)”开关（默认开启），避免未完成组柜的已分配货物干扰挑货。
  - 未出柜货物池新增分页（上一页/下一页 + 页码信息），缓解大数据量浏览压力。
  - 移除“柜次状态与确认”旧区块，避免与拼柜工作台操作重复。
  - 放大拼柜勾选框尺寸，提升鼠标点击可用性。
  - 柜内“更新体积/移除”增加二次确认，并在成功后弹窗提示，强化操作反馈。
  - 新建柜次默认运费改为 `6100`（界面输入“元/68CBM”），提交时自动换算为 `default_price_per_m3` 入库。
  - 后端兜底：若创建柜次未传 `default_price_per_m3`，默认按 `6100/68` 写入。
- 更新原因：针对拼柜实操阶段的可视性、可操作性和默认业务参数进行修正，降低误操作与学习成本。

### 23. 查询可改客户 + 占用信息强化 + agent 文档整理
- 时间：2026-04-04
- 更新内容：
  - 在“手动修改在库商品”中支持修改客户：
    - 可直接填写 `客户ID`；
    - 或填写 `客户名` 自动解析为客户ID（调用 `/customer-resolve`）后更新。
  - 拼柜工作台“当前占用体积/容量”改为大号信息卡，提升关键指标可见性。
  - `agent/` 目录结构整理：
    - 新增索引：`agent/README.md`
    - 需求文档移动到：`agent/requirements/`
    - 导入规范移动到：`agent/import/`
    - 导入报告移动到：`agent/reports/`
    - 同步修正：
      - `agent/AGENTS.md` 需求文档路径
      - `scripts/run_import_loop.py` 默认报告路径（并确保目录自动创建）
- 更新原因：满足“查询时可改客户、关键体积信息更醒目、agent 文档更清晰可维护”的新一轮验收反馈。

### 24. 结算规则调整（仅 CONFIRMED 柜次，按柜次单价计费）
- 时间：2026-04-04
- 更新内容：
  - 结算入口 `generate_statement` 新增状态校验：
    - 仅允许对 `CONFIRMED` 柜次生成结算单；
    - 非 `CONFIRMED` 柜次直接拒绝结算。
  - 运费计算规则调整为：
    - `客户运费 = 该客户在柜体积(cbm_total) × 柜次单价(default_price_per_m3)`；
    - 不再按客户价格规则覆盖柜次单价。
  - 补充数据有效性校验：
    - 柜次单价为空时拒绝结算；
    - 柜内无货时拒绝结算。
- 更新原因：对齐“已 CONFIRMED 的货柜才可结算，且运费按柜次单价计算”的业务规则。

### 25. 结算界面新增货柜列表选择
- 时间：2026-04-04
- 更新内容：
  - “生成结算单”区域将 `柜次ID` 文本输入改为“货柜下拉列表”。
  - 下拉列表自动加载当前 `CONFIRMED` 状态货柜，便于直接选择要结算的柜次。
  - 柜次列表刷新时同步刷新结算下拉选项，保持界面数据一致。
- 更新原因：减少手工输入柜次ID，降低操作错误并提升结算效率。

### 26. 列表交互统一 + 结算容错与反过账
- 时间：2026-04-04
- 更新内容：
  - 结算货柜下拉格式增强：
    - 选项显示 `柜次ID / 柜号 / 已用CBM/容量CBM / 单价`，便于准确选择。
  - 统一列表交互（本版覆盖核心列表）：
    - 在库、总账、客户、柜次、结算单、导入批次、拼柜已选货物、拼柜货物池均提供：
      - 条数显示（总条数+当前页）
      - 翻页（上一页/下一页）
      - 展开/收起
    - 其中客户/柜次/结算单提供每页条数选择。
  - 修复重复结算同柜时报错：
    - 结算单号改为自动避重（如 `STM-...`, `STM-...-2`, `STM-...-3`），不再触发唯一键异常。
  - 过账支持撤销：
    - 新增后端接口 `POST /settlements/<id>/unpost`；
    - 新增“撤销过账”前端表单；
    - 状态规则：仅 `DRAFT` 可过账，仅 `POSTED` 可撤销过账。
- 更新原因：满足“列表统一可操作”“重复结算不报错”“过账可撤销”的验收要求。

### 27. 柜次明细展开 + 柜单导出 + 客户货物查询
- 时间：2026-04-05
- 更新内容：
  - 新增柜次明细能力：
    - 后端接口 `GET /containers/<id>/details`，返回柜次头信息、客户汇总（体积/金额）、货物明细（含单件体积与金额）。
    - 拼柜页面新增“柜次明细（展开查看）”区域，支持查看汇总与明细、分页与收起。
  - 新增柜单导出能力：
    - 后端接口 `POST /exports/container/<id>`，支持 `xlsx/pdf`。
    - 导入导出页新增“导出柜单”表单。
    - 拼柜明细区新增“导出柜单 Excel/PDF”快捷按钮。
  - 客户维度货物查询：
    - 新增接口 `GET /customer-items`（支持 `customer_id/customer_name`、状态筛选、排序字段与方向）。
    - 客户页新增“客户货物查询”表，展示是否入柜、所在柜、发货内容，并支持分页/收起。
  - 结算货柜下拉增强：
    - 显示 `柜次ID/柜号/已用CBM-容量/单价`，降低选错概率。
  - 冒烟测试扩展：
    - 增加柜次明细接口、柜单导出、客户货物查询验证。
- 更新原因：完成“柜次明细可展开并可导出”“入库/柜单/账单可导出”“客户可查询货物入柜与发货情况”的剩余核心需求。

### 28. 导出完善与批次撤回统计修正
- 时间：2026-04-05
- 更新内容：
  - 入库批次列表新增“撤回数量”显示：
    - 在“成功/总行”后追加“撤回 N”；
    - 统计口径为 `success_rows - 当前批次现存货物数`，用于反映单条撤回/批次撤回结果。
  - 修复柜单 PDF 中文显示问题：
    - PDF 导出启用 `STSong-Light` 中文字体，避免客户名/品名中文乱码或显示异常。
  - 导出内容与可读性增强（Excel/PDF）：
    - 入库、库存、总账、账单、柜单导出均扩充关键字段，减少信息缺失；
    - Excel 增加统一样式（表头高亮、边框、冻结首行、列宽优化）；
    - PDF 标题与列头改为更贴近业务对账的中文表达，并优化排版对齐。
  - 批次整批撤销行为修正：
    - 批次撤销不再直接把 `success_rows` 强制置零，避免统计失真。
- 更新原因：响应“撤回数量可见、PDF 内容正确、导出条目更全且更易读”的验收反馈。

### 29. Windows 一键下载/更新脚本
- 时间：2026-04-05
- 更新内容：
  - 新增 `update_from_github.bat`：
    - 仅分发这一个脚本即可使用；
    - 首次运行自动下载项目，后续运行自动覆盖更新；
    - 默认更新后自动启动系统；
    - 支持 `--sync-only`（仅更新）与 `--start-only`（仅启动）参数。
  - 稳定性增强：
    - 修复双击闪退，脚本结束前固定停留并输出日志 `update_from_github.log`；
    - 启动失败时明确回显错误码；
    - 改进项目目录识别，避免 `DataBase\\DataBase` 误嵌套；
    - 启动流程改为脚本内联执行（创建 `.venv`、安装依赖、初始化数据库、运行 `run.py`），不依赖 `start_windows.bat`；
    - 更新路径切换为 GitHub ZIP 主路径（Git-free），并提供主/备下载地址提高成功率。
  - 运维数据保护：
    - 覆盖更新时保留本地数据和运行目录（如 `.canyu_data`、`.venv`、`backups`、`exports`、`dist`）；
    - 保留关键本地文件（`.env`、`shipping.db`、`update_from_github.log`），避免升级覆盖业务数据。
  - README 补充“单脚本分发与更新”使用说明与排查提示。
- 更新原因：落实“只发一个脚本给用户即可下载/更新并运行”的要求，降低分发与维护成本。

### 30. 项目清理（移除过时脚本与文档修正）
- 时间：2026-04-05
- 更新内容：
  - 删除过时脚本：`scripts/bootstrap_customers_from_data.py`。
    - 原因：其能力已被 `scripts/run_import_loop.py` 内置的客户预置逻辑覆盖，避免重复维护。
  - 修正文档中过时描述：
    - `README.md` 的功能清单改为当前真实状态（导入可执行、结算可反过账、柜单/账单支持 PDF、查询能力增强）。
- 更新原因：降低冗余脚本带来的使用歧义，保持文档与当前系统能力一致。

### 31. AGENTS 文档摘要化（降低上下文占用）
- 时间：2026-04-05
- 更新内容：
  - 重写 `agent/AGENTS.md` 为“摘要版”：
    - 明确项目目标与高优先业务约束；
    - 固化后续 agent 必读文件顺序；
    - 增加开发原则、文档/版本要求与任务结束自检清单。
- 更新原因：减少长上下文重复读取成本，提升后续 agent 进入任务时的效率与一致性。

### 32. UI 体验改进（登录、二级导航、可读性与反馈）
- 时间：2026-04-05
- 更新内容：
  - 登录体验升级：
    - 重做 `login.html` 视觉样式（居中卡片、层次化标题、更明确输入引导）。
    - 登录后直接进入业务面板：`/` 与表单登录成功统一跳转到 `/ui/dashboard`。
  - 页面结构优化：
    - 在查询、客户、拼柜、结算、导入导出 5 个一级模块中增加“二级导航”切换，减少单页信息拥挤。
    - 二级划分按强相关功能分组（如“在库与货物 / 总账查询”“拼柜工作台 / 柜次管理 / 柜次明细”）。
  - 可读性增强：
    - 除在库货物主表外，其余列表字体增大；
    - 状态字段改为轻量状态徽标（如 `IN_STOCK/ALLOCATED/SHIPPED`、`DRAFT/CONFIRMED/REVOKED/POSTED`）。
    - 总账“净余额”增加红绿标识（正绿负红）以强化财务识别。
  - 拼柜工作台视觉强化：
    - 左右两侧表格区采用不同底色，提升“待选货物池/柜内已选货物”区分度。
  - 按钮反馈增强：
    - 对部分查询/确认动作增加弹窗反馈（查询完成、组柜确认、撤回、结算生成/过账/反过账、批次查询等）。
- 更新原因：响应验收反馈，提升界面高级感、操作引导和一线用户阅读效率，降低误操作与学习成本。

### 33. 导入区与日期交互统一改造（路径/刷新/日期格式/日历）
- 时间：2026-04-05
- 更新内容：
  - 导入区交互修正：
    - 执行导入后自动清空文件选择与路径状态（演练与正式导入均生效），避免误重复提交。
    - 预览与执行改为共享单一路径状态展示，去除重复路径输入。
    - 导入卡片按步骤拆分为“上传并预览 / 执行导入 / 批次管理（查询与撤销）”。
    - 导入成功后立即刷新导入批次列表、总览数据与在库查询数据。
  - 日期格式统一（显示与输入）：
    - 后端 `infer_inbound_date` 增强规范化，统一输出 `YYYY-MM-DD`，兼容 `YYYY/MM/DD`、`YYYY年MM月DD日` 等输入。
    - 前端新增统一日期解析/格式化工具，页面所有日期展示统一为 `YYYY-MM-DD`，时间字段统一为 `YYYY-MM-DD HH:MM:SS`。
    - 日期输入框从原生本地化展示改为统一 `YYYY-MM-DD` 文本输入，并在失焦/提交前自动规范化。
  - 日历选择能力恢复：
    - 每个日期输入框增加日历按钮（`📅`），可调用原生日历选择器并回填统一格式。
- 更新原因：统一日期体验并兼顾“可读性一致 + 日历选择便利”，同时减少导入误操作与页面信息重复。

### 34. 货柜管理细化（运费列/柜号快速修改/按柜号查明细）
- 时间：2026-04-05
- 更新内容：
  - 一级导航名称优化：`拼柜` 调整为 `货柜管理`。
  - 柜次管理列表新增“运费(元/68CBM)”列（由 `default_price_per_m3 * 68` 计算），便于直接核对报价口径。
  - 柜号快速修改：
    - 柜次列表每行柜号后新增编辑图标（✏️）；
    - 前端点击后可直接输入新柜号；
    - 后端新增 `PUT /containers/<id>`，支持更新柜号并校验唯一性。
  - 柜次明细查询增强：
    - 查询条件由“仅柜次ID”扩展为“柜次ID或柜号”；
    - 前端可输入柜号自动定位到对应柜次后加载明细。
- 更新原因：提升货柜管理场景的操作效率与信息完整度，减少人工换算与多步骤查找。

### 35. 货柜管理微调（柜号编辑图标尺寸 + 明细查询双输入）
- 时间：2026-04-05
- 更新内容：
  - 柜次管理列表中的“修改柜号”图标缩小（约 50%），降低视觉干扰。
  - 柜次明细查询从单一混合输入改为双输入框：
    - `柜次ID`（仅数字）
    - `柜号`（文本）
  - 查询规则调整为“二选一”，避免纯数字柜号被误判为柜次 ID。
- 更新原因：修复验收阶段的可用性问题，提升查询准确性与界面一致性。

### 36. 客户管理与结算币种展示改造（默认美元 + 汇率切换）
- 时间：2026-04-05
- 更新内容：
  - 客户模块改名：`客户与价格` -> `客户管理`。
  - 客户侧简化：
    - 移除“默认单价”与“价格规则”界面展示；
    - 客户档案列表改为显示“净余额”，并保留红绿正负标识。
  - 币种与汇率显示能力：
    - 页面顶部新增汇率输入（USD→CNY，默认 `7.0`）；
    - 新增一键显示切换（USD/CNY）；
    - 主要金额字段统一按当前显示币种动态换算展示（入库金额、定金、总账、柜次运费、柜次明细金额等）。
  - 单位文案调整：
    - 关键金额输入与表头补充币种标识（如 `PRICE(USD)`、`金额(USD)`、`运费(USD/68CBM)`）。
  - 后端默认币种调整：
    - 付款默认币种改为 `USD`；
    - 结算单默认币种改为 `USD`；
    - 价格规则接口默认币种改为 `USD`（兼容旧接口）。
- 更新原因：落实“未标注人民币则按美元处理”的业务口径，并提供可配置汇率与美元/人民币一键切换查看能力。

### 37. 客户与结算补充优化（单位后缀收敛 + 柜次结算状态同步 + 过账全量刷新）
- 时间：2026-04-05
- 更新内容：
  - 金额展示优化：
    - 仅在表头/标题处显示单位（USD/CNY），具体数值单元格不再追加 `USD/CNY` 后缀，提升表格可读性。
  - 柜次状态与结算同步：
    - 柜次列表新增 `status_display` 展示逻辑：
      - `CONFIRMED` 且存在 `POSTED` 结算单 -> 显示 `POSTED`
      - `CONFIRMED` 且存在 `DRAFT` 结算单 -> 显示 `SETTLING`
      - 其他沿用原柜次状态
    - 柜次列表、总览柜次、结算下拉均使用同步后的展示状态，便于直观看到“已结算”。
  - 结算操作反馈与刷新强化：
    - 生成结算 / 过账 / 撤销过账增加确认弹窗；
    - 成功后统一触发全量视图刷新（客户、柜次、结算、总账、在库、工作台、总览），避免状态滞后。
- 更新原因：满足“状态更直观、过账确认更强、操作后自动同步全部列表”的验收要求。

### 38. 资金结算重构（按货柜驱动）
- 时间：2026-04-05
- 更新内容：
  - 资金结算模块改造：
    - 一级导航文案由 `结算` 调整为 `资金结算`。
    - 二级导航拆分为 `定金登记` 与 `结算工作台`，其中“生成/过账/结算列表”统一归入“结算工作台”。
  - 结算流程改为按货柜状态操作：
    - 生成结算：从 `CONFIRMED` 货柜下拉选择并生成；
    - 过账结算：从 `SETTLING` 货柜下拉选择并过账（进入 `POSTED`）；
    - 撤销生成：从 `SETTLING` 货柜下拉选择并撤销（回退到无结算状态）。
    - 各操作成功后统一全量刷新并保留弹窗反馈。
  - 结算单隐式绑定货柜：
    - 新增后端接口 `POST /exports/statement/by-container/<container_id>`，按货柜导出最新有效结算单；
    - 导出区“导出账单”由手输结算单 ID 改为选择货柜导出。
  - 后端能力补齐：
    - 新增 `post_statement_by_container` 与 `revoke_draft_statement_by_container` 服务及对应 API；
    - 生成结算时拦截同货柜重复有效结算（避免重复生成）。
- 更新原因：落实“以货柜为结算主对象、减少手输单据 ID、状态可视且可撤销”的业务要求，降低操作复杂度与误操作风险。

### 39. 资金结算补充优化（撤销过账 + 定金列表 + 全量刷新）
- 时间：2026-04-05
- 更新内容：
  - 过账撤销增强：
    - 新增后端服务 `unpost_statement_by_container` 与接口 `POST /settlements/container/<container_id>/unpost`；
    - 前端“结算工作台”新增“撤销过账（POSTED）”入口，按货柜操作并回退到 `SETTLING`（对应结算单回 `DRAFT`）。
  - 定金核验列表：
    - 新增后端接口 `GET /payments`（按时间倒序返回定金流水）；
    - “定金登记”二级功能下新增“定金流水列表”，可一键刷新核验。
  - 刷新时效性加强：
    - 对货柜状态相关操作（建柜、装柜、移除、确认、撤回、修改柜号、改装柜 CBM）统一改为触发 `refreshAllViews()`；
    - 对定金登记与结算流转（生成/过账/撤销生成/撤销过账）统一执行全量刷新，避免页面局部滞后造成误判。
- 更新原因：落实“状态实时同步、可核验、可回退”的验收反馈，降低业务操作中的信息延迟风险。

### 40. 查询页总 CBM 展示优化（不受客户筛选影响）
- 时间：2026-04-05
- 更新内容：
  - 查询页“在库查询”分页信息新增 `总CBM` 展示。
  - 总CBM统计口径调整为“当前后端查询返回的全量结果”：
    - 先按入库日期/批次/在库状态等查询条件取全量；
    - 再进行前端关键字筛选（客户/单号/品名）仅影响表格展示条目，不影响总CBM显示。
  - 统计字段优先使用 `cbm_final`，缺省回退 `cbm_calculated`，并统一保留 3 位小数展示。
- 更新原因：满足“即使选定客户筛选，查询界面仍需展示总CBM（全量口径）”的核对需求，避免筛选后误判总体积。

### 41. 查询页统计显示微调（分页信息字体 + 关键字筛选后总CBM）
- 时间：2026-04-05
- 更新内容：
  - 在库查询分页信息（`共x条，当前第...`）字体由 `small` 调整为更醒目的常规字号并加粗，提升可读性。
  - 总CBM统计口径调整为“关键字筛选后的结果”：
    - 先按入库日期/批次/在库状态获取结果；
    - 若填写关键字（单号/客户/品名），先筛选表格数据，再按筛选后列表计算总CBM。
- 更新原因：响应最新验收反馈，确保查询统计与用户当前看到的筛选结果一致，同时提升信息可见性。

### 42. 导入增强（支持 CTN 合并单元格分组合并）
- 时间：2026-04-05
- 更新内容：
  - Excel 读取阶段新增合并单元格范围采集（`.xlsx` 与 `.xls`）。
  - 入库解析新增“按 CTN 列合并区间归并行”能力：
    - 当 `CTN/CTNS` 列存在纵向合并（例如 3 行合并为 1，值为 `1`）时，将该区间命中的多行货物归并为 1 条记录；
    - 归并后 `item_name_cn` 按行顺序使用 `+` 连接（如 `A+B+C`）；
    - `QTY`、`PRICE` 与 `T.PRICE` 改为按归并区间内各行求和；
    - 其余字段沿用首行内容作为代表值；
    - 在 `raw_row` 中写入 `MERGED_CARTON_ROWS` 便于追踪来源行号。
- 更新原因：满足“同一箱内多行货物在导入时统一计算”的业务规则，减少人工后处理和重复记录。

### 43. 装柜工作台增强（显示 CTN + 按 CTN 拆货）
- 时间：2026-04-05
- 更新内容：
  - 装柜工作台货物池新增 `CTN` 列，柜内已选货物表也同步显示 `CTN`，便于装柜时核对箱数。
  - 新增“按 CTN 拆货”能力（货物池行级按钮）：
    - 仅允许 `IN_STOCK` 且 `CTN>1` 的货物执行拆分；
    - 输入拆分箱数后，将原货物拆成两条在库记录（保留部分 + 新增部分）。
  - 拆分比例规则：
    - `CTNS` 按输入箱数拆分；
    - `QTY`、`PRICE`、`T.PRICE`、`CBM`（含 `cbm_override`）按 CTN 比例分割；
    - 两部分数值相加保持与拆分前一致（按字段精度做四舍五入，余量回留在原记录）。
  - 后端新增接口：`POST /inbound-items/<item_id>/split-by-cartons`。
- 更新原因：满足“装柜界面可见 CTN 并能按箱拆货”的业务操作诉求，减少人工拆分与重复录入。

### 44. 装柜工作台显示优化（货物池字段收敛 + 客户汇总 + 柜内去ID）
- 时间：2026-04-05
- 更新内容：
  - 未出柜货物池字段收敛为：`客户 / 品名 / CTNs / CBM`（保留勾选与操作列）。
  - 状态列按“隐藏已分配(ALLOCATED)”动态显示：
    - 勾选“隐藏已分配”时不显示状态列；
    - 未勾选时显示状态列。
  - 当前柜内货物表移除“货物ID”展示，其余信息保持可见并支持原有操作（改装柜体积、移除）。
  - 新增“未出柜货物池按客户汇总”表，显示每个客户的：
    - 总体积（总CBM）
    - 件数（按 CTN 汇总）
- 更新原因：减少装柜主视图信息噪音，突出核心决策字段，并增加客户维度总览能力。

### 45. 装柜工作台布局与客户汇总模式改造（可切换并支持加入柜次）
- 时间：2026-04-05
- 更新内容：
  - 工作台左右布局优化：未出柜货物池区域收窄，当前柜内货物区域加宽，提升操作区阅读空间。
  - 货物池新增“视图切换”：
    - `货物明细` 与 `按客户汇总` 二选一显示，不再同时展示。
  - 汇总模式能力补齐：
    - 支持勾选客户并一键加入柜次（自动将该客户当前可入柜货物批量加入）；
    - 保留分页与全选本页能力。
  - 状态列显示规则延续到切换模式：
    - 勾选“隐藏已分配”时，状态列隐藏；
    - 取消勾选时，状态列显示。
- 更新原因：修复“布局不合理、汇总视图不可操作”的使用痛点，确保汇总模式可直接用于装柜作业。

### 46. 货柜工作台视觉细节修正（列宽/按钮/汇总勾选）
- 时间：2026-04-05
- 更新内容：
  - 货物池列宽继续收敛，重点压缩“品名”列展示长度，避免过长占据主要空间。
  - “拆货”按钮改为小尺寸按钮，并在“操作”列居中对齐，提升表头与内容对齐一致性。
  - 客户汇总模式勾选框放大，并修正显示对齐问题，避免被客户文本遮挡。
  - 客户汇总模式下“件数”列宽缩小，减少无效留白。
- 更新原因：响应验收中的可读性与对齐问题反馈，进一步提升装柜工作台操作效率。

### 47. 新增 2026data 每日收货清单格式采样文档
- 时间：2026-04-05
- 更新内容：
  - 新增文档：`/agent/format/daily_receipts_2026data_format_sample.md`。
  - 对 `./2026data/每日收货清单` 目录下 Excel 样本进行格式采样与归纳，覆盖：
    - 目录与文件概况（文件数、月份层级）；
    - sheet/标题行位置分布；
    - 表头字段稳定性与映射；
    - 行级空值特征、续行模式、合并单元格情况；
    - 导入建议与结论。
- 更新原因：为后续导入规则优化与验收核对提供统一、可复用的数据格式参考基线。

### 48. 新增 2026data 客户清单（多 Sheet）格式采样文档
- 时间：2026-04-05
- 更新内容：
  - 新增文档：`/agent/format/customer_receipts_2026data_format_sample.md`。
  - 针对 `./2026data` 下客户侧收货清单文件（排除“每日收货清单/装柜清单/店面对账”）进行采样归纳，覆盖：
    - 多 Sheet（月度）结构特征；
    - `TEL` 与 `位置` 字段的同义映射说明（统一归入 `position_or_tel`）；
    - `CBM FREIGHT` 行作为“已出仓分段标记”的识别与处理建议。
  - 文档中同步纳入业务补充口径：
    - 每月分 sheet；
    - `TEL` 实为“位置”；
    - 出现 `...CBM FREIGHT...` 行表示其上方货物已出仓，反之未出仓。
- 更新原因：为客户侧历史多月台账文件的稳定解析与状态判定提供明确样本依据。

### 49. 文档目录整理（新增 format 文件夹 + pipeline 对齐）
- 时间：2026-04-05
- 更新内容：
  - 新建目录：`/agent/format/`，并将两份格式采样文档迁移至该目录：
    - `/agent/format/daily_receipts_2026data_format_sample.md`
    - `/agent/format/customer_receipts_2026data_format_sample.md`
  - 更新 `pipeline.md`：
    - 修正“收获清单”表述为“收货清单”；
    - 将格式文档引用路径统一为 `/agent/format/`。
  - 更新文档索引：
    - `AGENTS.md` 增加 `pipeline.md` 与 `format/` 入口；
    - `agent/README.md` 增加“流程与格式”章节并纳入新路径。
- 更新原因：统一文档组织结构，降低后续查找成本并避免路径歧义。

### 50. 客户电话回填（基于客户清单第一列纯数字）+ 格式文档补充
- 时间：2026-04-05
- 更新内容：
  - 按客户清单采样口径执行电话回填：
    - 数据源：`./2026data` 客户侧收货清单（排除“每日收货清单/装柜清单/店面对账”）；
    - 提取规则：各 sheet 第一列中“纯数字”值作为电话候选（兼容 `.xls` 数值如 `8092882965.0`）；
    - 归并规则：按客户目录聚合后取“出现频次最高”的号码写入 `customers.phone`。
  - 本次回填结果：
    - 客户目录总数：223；
    - 可提取电话目录：210；
    - 成功匹配并更新客户：12；
    - 未匹配客户目录：198（目录名与当前客户主档/别名不一致需后续补映射）。
  - 更新格式文档：`/agent/format/customer_receipts_2026data_format_sample.md`
    - 补充“电话字段通常位于第一列且为纯数字”的结构说明与解析建议。
- 更新原因：落实“客户电话字段补全”要求，并将电话字段规则纳入客户清单格式基线文档。

### 51. 历史在库回填（脚本新增、执行与规则优化汇总）
- 时间：2026-04-05
- 更新内容：
  - 新增脚本：`/scripts/import_historical_in_stock.py`，用于一次性回填历史“仍在库”数据。
  - 基础流程：
    - 扫描 `2026data` 客户目录（排除“每日收货清单/装柜清单/店面对账”）；
    - 每目录选一个 Excel（优先“收货清单”，否则取最新）；
    - 识别 `CBM FREIGHT` 为出仓/结算标记，只提取“最后标记行之后”的在库货物；
    - 入库写入 `inbound_items`（`IN_STOCK`），并规范化 `CTNS/QTY/PRICE/T.PRICE/CBM`；
    - 支持 `--dry-run` / `--apply` / `--customer` / `--limit` / `--inbound-date`。
  - 规则优化（最终口径）：
    - sheet 选择从“固定最后一个”改为“从后往前第一个有效 sheet”；
    - 若某 sheet 第一行为空，则该 sheet 直接视为空；
    - 若全部 sheet 为空，则保留客户扫描并跳过（不计解析失败）。
  - 可观测性增强：
    - 增加 `folders_all_sheets_empty` 统计项；
    - `--verbose` 输出 `[SKIP_ALL_EMPTY_SHEETS] 客户名`。
  - 执行与验证结果：
    - `--apply` 首次导入：`IN_STOCK` 424 条，自动建客户 45，电话回填 44；
    - 规则优化后 `--dry-run`：`folders_parse_failed=0`，`folders_all_sheets_empty=1`，`rows_candidate=424`。
- 更新原因：将历史客户台账中未出柜货物稳定回灌数据库，并消除多 sheet/空 sheet 导致的误判。

### 52. 历史回填补充规则（无在库也入客户与电话）
- 时间：2026-04-05
- 更新内容：
  - 调整 `scripts/import_historical_in_stock.py`：
    - 只要客户目录存在可选收货清单文件，就先确保客户主档入库；
    - 即使该客户“无在库货物”，仍执行电话回填（若主档电话为空且文件可提取号码）；
    - 货物明细仍仅导入“仍在库”部分，不改变原有过滤口径。
- 更新原因：满足“无在库货物也要入客户与电话号码”的测试与建档要求。

### 53. 柜次出仓/运费结算标记行识别规则汇总更新
- 时间：2026-04-06
- 更新内容：
  - `scripts/import_historical_in_stock.py` 标记行识别从“关键字+固定列位”逐步调整为“柜号优先”：
    - 不再强依赖 `CBM FREIGHT/FEIGHT` 是否出现；
    - 柜号不再要求位于首列或固定列位（兼容第 7 列及更靠后位置）；
    - 只要行内任意单元格命中柜号 token 即识别为标记行；
    - 柜号格式扩展为 `4~5字母 + 6~7数字`（如 `JXLU4327901`、`TRLU711969`）。
  - 样本覆盖：
    - `HAPPYLADY EBUTRO`：修复误判“仍在库”；
    - `OSAMA`：兼容列位右移；
    - `BASIL`：兼容缺失 `CBM FREIGHT` 关键字的标记行；
    - `HINDATU`：兼容 4 字母 + 6 数字柜号。
  - 文档同步：
    - 更新 `agent/format/customer_receipts_2026data_format_sample.md`，强调“柜号驱动识别”与柜号格式范围。
- 更新原因：适配员工填表结构不一致导致的多种标记行变体，减少在库误判/漏判。

### 54. 尺寸字段与合并货物处理增强（导入/拆货/编辑/导出）
- 时间：2026-04-06
- 更新内容：
  - 导入解析增强（`app/services/importer.py`）：
    - 若 `长/宽/高` 未显式表头映射，默认读取 `CBM` 列后 3 列为 `length_cm/width_cm/height_cm`（隐式存储）；
    - 合并字段货物（按 CTN 合并区间）新增标记：
      - 商品名输出为 `A+B+...*`（尾部 `*`）；
      - `raw_row` 写入 `MERGED_CARTON=1` 与 `MERGED_CARTON_ROWS`；
      - `qty/PRICE/T.PRICE` 求和；
      - `cbm_calculated` 同步求和（即使 CBM 单元格非合并）；
      - `length/width/height` 保留首行值。
    - 入库写入时为合并货物加 `remark='MERGED_CARTON'`。
  - 拆货增强（`app/services/containers.py` + `/inbound-items/<id>/split-by-cartons`）：
    - 支持传入 `length_cm/width_cm/height_cm` 作为拆后尺寸；
    - 若不传则默认沿用原尺寸；
    - 若拆分来源为合并货物，返回 `source_is_merged=true` 且备注标注 `[SPLIT_FROM_MERGED]`。
  - 编辑增强（`app/templates/dashboard.html` + `app/services/inbound.py`）：
    - 在“手动修改在库商品”中加入 `长/宽/高` 字段；
    - 支持任意单项尺寸更新，自动用旧值补齐并重算 `cbm_calculated`。
  - 导出增强（`app/services/reports.py`）：
    - `daily_inbound`、`inventory`、`container_manifest` Excel 增加 `length_cm/width_cm/height_cm` 列。
  - 列表显示策略：
    - 日常列表不新增尺寸列，保持当前简洁展示。
- 更新原因：满足“尺寸隐式导入、合并货物可识别、拆货可指定尺寸、编辑导出包含尺寸”的新业务口径。

### 56. 历史回填新增文件年份过滤（忽略 2026 年前修改文件）
- 时间：2026-04-06
- 更新内容：
  - `scripts/import_historical_in_stock.py` 新增参数 `--min-file-year`；
  - 客户目录选文件时，若文件修改年份小于指定年份则忽略；
  - 用于执行“仅导入近期（如 2026 年）维护过的客户文件”场景。
- 更新原因：满足“文件修改日期在 2026 年之前则忽略”的全量导入口径。

### 57. 入库日期识别改造（优先取 SHOP NO 左侧批次日期）
- 时间：2026-04-06
- 更新内容：
  - 调整 `scripts/import_historical_in_stock.py` 入库日期来源：
    - 优先读取每批次 `SHOP NO` 表头左侧单元格（位于电话下方）作为 `inbound_date`；
    - 支持 Excel 序列日期（如 `46056 -> 2026-02-03`）与常见日期字符串；
    - 若该批次未识别到日期，则回退文件修改日期；
    - 若命令行传 `--inbound-date`，仍作为最高优先级覆盖。
  - 解析过程支持同一 sheet 多批次表头切换，按段更新批次日期。
- 更新原因：修复“入库日期使用文件修改日期导致批次日期不准”的问题，使导入日期贴合清单结构。

### 58. 回填范围过滤（首字母为中文的文件夹忽略）
- 时间：2026-04-06
- 更新内容：
  - 调整 `scripts/import_historical_in_stock.py` 客户目录扫描规则：
    - 顶级目录名首字符为中文的文件夹不纳入回填范围；
    - 继续保留“每日收货清单/装柜清单/店面对账”排除规则。
- 更新原因：满足“首字母为中文的文件夹都不用管”的导入口径，减少非客户目录干扰。

### 59. 货柜新增柜主字段（主单客户）并接入拼柜工作台
- 时间：2026-04-06
- 更新内容：
  - 数据层：
    - `containers` 新增 `master_customer_id` 字段（主单客户/柜主）；
    - `schema.sql` 同步字段定义，`init_db()` 增加旧库补列迁移（缺列时自动 `ALTER TABLE`）。
  - 后端能力：
    - `list_containers` / `container_manifest` 返回 `master_customer_id` 与 `master_customer_name`；
    - 新增接口：`PUT /containers/<id>/master-customer`，支持设置/清空柜主。
  - 拼柜工作台（前端）：
    - 实时显示“当前柜主”，默认“未选择”；
    - 新增“柜主（输入客户名）+ 设置柜主”操作，按客户名解析后写入柜主；
    - 柜次下拉项附带显示柜主信息，便于快速核对。
  - 规则补充（校验与自动清空）：
    - 设置柜主时，要求该客户在当前柜内确有货物，否则拒绝设置；
    - 移除货物/撤回柜次后自动校验当前柜主，若柜内已无该客户货物则自动清空柜主；
    - 柜主输入区样式对齐优化，保证输入框与“设置柜主”按钮视觉对齐。
- 更新原因：满足“每个货柜需有主单客户/柜主，并在拼柜工作台可实时查看与按名称设置”的业务需求。

### 60. 新增“设置”一级功能（账户设置下沉 + 工作目录配置）
- 时间：2026-04-06
- 更新内容：
  - 前端导航改造：
    - 侧边栏新增一级菜单“设置”；
    - 原“账户”页下沉为“设置”的二级功能“账户设置”（修改密码）；
    - 新增二级功能“工作目录”用于配置客户文件与每日清单所在目录。
  - 后端新增设置能力：
    - 新增 `app/services/ui_settings.py`（JSON 持久化，保存工作目录）；
    - 新增接口：
      - `GET /settings`
      - `PUT /settings/work-dir`
      - `POST /settings/work-dir/pick`（通过系统文件管理器选择目录）
      - `GET /import/inbound/workdir-files`（列出工作目录下可用“收货清单”Excel）。
  - 手动导入改造：
    - 在“手动导入收货清单”新增“从工作目录选择（优先）”步骤；
    - 可直接选择工作目录内文件并写入导入路径，再执行预览/导入。
- 更新原因：满足“账户设置并入设置菜单 + 新增工作目录配置 + 手动导入优先从工作目录选择”的功能需求。

### 61. 导入新增二级功能：工作目录历史在库数据导入
- 时间：2026-04-06
- 更新内容：
  - 在“导入导出”中新增二级功能“历史在库导入”：
    - 基于工作目录扫描客户文件夹；
    - 复用既有历史回填规则识别“仍在库”数据并导入。
  - 后端新增接口：
    - `POST /import/historical-in-stock/execute`
    - 参数支持 `dry_run`、`min_file_year`，默认使用设置中的工作目录。
  - 手动导入页展示工作目录提示，并支持一键执行历史回填（演练/写库）。
- 更新原因：满足“在导入里增加历史在库数据导入二级功能”的需求，避免重复脚本手工操作。

### 62. 历史在库导入交互增强（运行中反馈弹窗）
- 时间：2026-04-06
- 更新内容：
  - 历史在库导入执行时新增“运行中”反馈弹窗：
    - 开始执行后立即弹窗提示“正在导入”；
    - 运行期间隐藏关闭按钮，避免误判为已结束；
    - 任务结束后更新弹窗内容并显示“关闭”按钮。
- 失败场景同样在弹窗中展示错误信息并允许关闭。
- 更新原因：提升“执行历史在库导入”过程的确认感与可见性，降低等待过程中的不确定性。

### 63. 新增“文件同步”一级功能（收货同步 / 出仓同步 / 月份更新）
- 时间：2026-04-06
- 更新内容：
  - 后端新增文件同步服务 `app/services/file_sync.py` 并接入路由：
    - `GET /sync/receipts/batches`、`POST /sync/receipts/batch/<batch_id>`（收货同步）
    - `GET /sync/outbound/containers`、`POST /sync/outbound/container/<container_id>`（出仓同步）
    - `PUT /sync/monthly/settings`、`POST /sync/monthly/execute`、`GET /sync/monthly/auto-status`（月份更新）
  - 新增同步状态持久字段（自动补列）：
    - `import_batches.receipt_synced_at`
    - `containers.outbound_synced_at`
  - 设置项扩展：
    - `ui_settings` 新增 `monthly_auto_enabled`、`monthly_last_run_ym`。
  - 前端新增一级菜单“文件同步”及三个二级功能：
    - 收货同步：显示导入批次是否已同步，可按批次执行同步；
    - 出仓同步：显示已确认柜次是否已同步，可按柜次执行同步；
    - 月份更新：自动分 Sheet 开关 + 手动更新（含确认弹窗）。
  - 交互增强：
    - 收货/出仓/月份更新均加入运行中反馈弹窗；
    - 自动月份更新在每月 1 日首次触发前先弹窗确认；
    - 月份 Sheet 命名规范化为 `yyyy mm`（如 `2026 04`）。
- 更新原因：落实 pipeline 中“导入后回写客户文件、出仓后回写结算标记、按月分 Sheet”的闭环流程。

### 64. 文件同步增强（客户名异常检测 + 别名导出 + 月份 Sheet 模糊匹配）
- 时间：2026-04-07
- 更新内容：
  - `app/services/file_sync.py` 新增客户解析与文件夹匹配增强：
    - 导出时优先按 `customer_id` 解析主名与全部激活别名（`customers + customer_aliases`）；
    - 文件夹查找支持“主名/别名候选 + 大小写/空格归一匹配”；
    - 若客户目录不存在，返回明确异常：`customer folder not found, please create folder 'xxx'`。
  - 适用范围：
    - 收货同步：`sync_receipts_by_batch`
    - 出仓同步到客户：`sync_outbound_container_to_customers`
  - 月份 sheet 识别增强：
    - 新增月份解析函数，`yyyy m`、`yyyy mm`、`yyyy-m`、`yyyy m月` 统一识别为同月；
    - `_ensure_month_sheet` 与 `monthly_create_sheet` 均改为先模糊匹配，命中时不重复建 sheet。
- 更新原因：解决“客户别名导致导出落错目录/目录不存在无提示”与“同月重复建 sheet（如 2026 4 与 2026 04）”问题。

### 65. Windows 启动/更新脚本中文命名与文档统一
- 时间：2026-04-07
- 更新内容：
  - 脚本重命名：
    - `start_windows.bat` -> `启动.bat`
    - `update_from_github.bat` -> `更新.bat`
  - 同步更新文档引用与脚本文案：
    - `README.md`
    - `agent/import/import_data.md`
    - `scripts/package_release.py`（发布说明模板）
    - `更新.bat`（脚本头部用法说明、日志名改为 `更新.log`）
  - 文档索引与格式说明补齐：
    - `agent/README.md` 新增装柜清单格式文档入口；
    - `agent/pipeline.md` 对齐“收货同步/出仓同步/月份更新”流程；
    - `agent/format/container_lists_2026data_format_sample.md` 按最新同步口径重写。
- 更新原因：统一一线用户使用命名，减少脚本入口混淆，并确保文档与当前实现保持一致。

### 66. 备份版本管理增强（备份列表 + 指定版本回滚）
- 时间：2026-04-07
- 更新内容：
  - 后端新增备份查询与回滚能力：
    - `GET /backups`：返回 `backups` 目录下备份文件列表（文件名、版本名、修改时间、大小）；
    - `POST /backups/restore`：按 `file_name` 回滚到指定备份版本。
  - 回滚实现方式：
    - 使用 SQLite 原生 `backup()` 从备份文件覆盖当前数据库；
    - 增加文件类型与路径安全校验（仅允许 `backups` 目录下 `.db` 文件）。
  - 备份命名调整：
    - 新生成备份文件名改为日期版本名（如 `20260407_214500.db`），便于按时间选择版本。
  - 前端“导出与备份”区新增：
    - “刷新备份列表”按钮；
    - 备份列表表格（显示版本/文件名/时间/大小）；
    - 每条备份的“回滚到该版本”按钮（带确认与执行反馈弹窗）。
- 更新原因：满足“可查看 backups 下备份并回滚到指定日期版本”的运维需求，提升问题恢复效率。

### 67. 启动/更新脚本兼容修复（中文名不可用问题）
- 时间：2026-04-07
- 更新内容：
  - 新增兼容入口脚本：
    - `start_windows.bat` -> 转调 `启动.bat`
    - `update_from_github.bat` -> 转调 `更新.bat`
  - `启动.bat` 增强：
    - 自动探测 `py/python`；
    - 创建虚拟环境、安装依赖、初始化数据库、启动服务均增加错误提示与停留。
  - `更新.bat` 修正：
    - 同步时保留日志文件同时兼容 `update_from_github.log` 与 `更新.log`，避免覆盖异常。
  - 文档补充：
    - `README.md` 标注中文/英文双入口脚本均可用。
- 更新原因：解决“脚本改中文名后部分环境无法使用”的兼容性问题，降低一线机器差异导致的启动失败率。

### 68. 设置新增“清空数据库”二级功能（管理员密码确认）
- 时间：2026-04-08
- 更新内容：
  - 后端新增接口：`POST /settings/clear-db`
    - 仅 `admin` 角色可用；
    - 必须输入当前管理员账号密码校验通过后才执行；
    - 清空业务数据表（客户/货物/柜次/结算/导入/日志等），保留用户账号；
    - 重建默认仓库记录，保证后续可继续入库。
  - 前端“设置”新增二级功能“数据库维护”：
    - 新增“管理员密码 + 清空数据库”表单；
    - 提供二次确认弹窗与执行结果反馈；
    - 执行后自动刷新总览与各业务列表。
- 更新原因：满足“在设置中提供可控清库能力，并要求管理员密码确认”的运维需求。

### 69. 导出/同步数值格式统一（CBM 1位小数，金额按人民币取整）
- 时间：2026-04-08
- 更新内容：
  - 统一规则：
    - CBM 输出统一保留 1 位小数；
    - 金额输出统一按“先换算成人民币，再四舍五入到整数”。
  - 导出模块（`app/services/reports.py`）：
    - 日入库、库存、总账、结算单、柜单（Excel/PDF）按上述口径输出；
    - 容器/结算相关金额字段改为人民币整数展示。
  - 文件同步模块（`app/services/file_sync.py`）：
    - 收货同步写入客户文件时，`PRICE/T.PRICE/定金` 改为人民币整数；
    - 出仓同步（客户文件与装柜清单）中 CBM 改为 1 位小数，运费改为人民币整数；
    - 对应单元格货币格式调整为人民币整数格式。
- 更新原因：对齐“所有导出与同步输出口径一致”的业务要求，降低对账误差与格式歧义。

### 70. 导出与收货同步分组排序优化（客户 -> SHOP NO + 分组空行）
- 时间：2026-04-08
- 更新内容：
  - 导出格式调整（参考 `agent/format` 口径）：
    - 明细类导出统一按 `customer -> shop_no` 排序；
    - 同一 `SHOP NO` 结束后插入 1 空行；
    - 同一客户结束后额外插入 1 空行。
  - 应用范围：
    - `export_daily_inbound_excel`
    - `export_inventory_excel`
    - `export_container_excel` 的货物明细段
  - 收货同步调整（`sync_receipts_by_batch`）：
    - 同客户内按 `SHOP NO` 排序写入；
    - 同一 `SHOP NO` 切换时插入 1 空行。
- 更新原因：满足“导出与同步按客户/店铺分组阅读”的现场核对需求，减少人工查找成本。

### 71. 导出版式对齐同步格式（货物=收货同步，客户=出仓同步）
- 时间：2026-04-09
- 更新内容：
  - 货物导出格式统一为“收货同步同款”：
    - 字段顺序改为：`客户, SHOP NO, TEL, ITEM NO, 品名, 材质, CTNS, QTY, PRICE, T.PRICE, 定金, CBM, 长, 宽, 高`；
    - 去除“状态”等非收货同步字段；
    - 列宽改为与收货同步一致的 A-O 固定宽度。
  - 应用范围：
    - `export_daily_inbound_excel`
    - `export_inventory_excel`
    - `export_container_excel` 的货物明细段
  - 账单/柜单客户格式统一为“出仓同步同款”：
    - 列顺序改为：`PHONE NUMBER, NAME, CBM, CTN, FREIGHT`；
    - 柜单导出与账单导出均按该客户块输出（柜主优先）。
  - 数据准备增强：
    - `container_manifest` 的客户汇总补充 `customer_phone` 与 `ctns`，用于导出客户块。
- 更新原因：满足“导出与同步格式一致”的验收要求，降低人工核对时的格式切换成本。

### 72. 导出补充：货物首列入库日期 + xlsx显示层四舍五入
- 时间：2026-04-09
- 更新内容：
  - 所有货物导出条目最左列增加“入库日期”。
  - `.xlsx` 导出改为“写入原始数值 + 单元格格式控制显示”：
    - 金额字段：写入人民币换算后的原始数值，显示格式为 `¥#,##0`（显示层四舍五入到整数）；
    - CBM 字段：写入原始数值，显示格式为 `0.0`（显示 1 位小数）。
  - 适用范围：
    - 日入库/库存/柜单/账单/总账的 xlsx 导出。
- 更新原因：纠正“写入层直接四舍五入”的口径偏差，改为“数据保真 + 显示层规范”的导出策略。

### 73. 币种口径修正（默认USD，仅结算行单格人民币）+ 柜单货物字段修复
- 时间：2026-04-09
- 更新内容：
  - 币种口径：
    - 除“客户文件结算行”中的人民币单元格外，其余导出/同步金额默认按 USD 输出；
    - 结算行维持：人民币一格 + 美元一格。
  - 柜单导出货物段修复：
    - 补齐并输出 `材质 / CTNS / QTY / PRICE / T.PRICE / 定金`；
    - 对齐收货同步同款字段顺序与列宽。
  - 容器数据准备补充：
    - `container_manifest` 查询补充 `material/carton_count/qty/unit_price/total_price/deposit_hint` 字段。
- 更新原因：修复币种误设与柜单货物字段缺失问题，确保导出内容与业务核对口径一致。

### 74. 历史在库导入：近两年 Sheet 英文别名自动识别并入库
- 时间：2026-04-09
- 更新内容：
  - 调整 `scripts/import_historical_in_stock.py`：
    - 在历史在库导入时，新增“别名扫描”流程；
    - 扫描所选客户收货文件中“近两年（按 sheet 名年份识别）”的 sheet；
    - 从第一列提取英文别名候选（仅支持英文字母与空格，过滤手机号/表头词/客户主名）；
    - 正式导入时将候选别名写入 `customer_aliases`（`AUTO_DETECT`）；
    - 演练模式仅统计 `aliases_would_upsert`，不写库。
  - 报表统计新增别名相关指标：
    - `alias_sheets_scanned`
    - `alias_sheets_skipped_unparsable_year`
    - `alias_sheets_skipped_outside_window`
    - `aliases_detected`
    - `aliases_would_upsert` / `aliases_upserted`
    - `aliases_detected_unbound_customer`
    - `alias_scan_failed`
- 更新原因：满足“历史在库导入自动发现近两年英文别名并入库”的补充需求，减少人工维护别名成本。

### 75. 新增“手动导入单条数据”功能
- 时间：2026-04-09
- 更新内容：
  - 后端新增接口：`POST /import/inbound/manual-item`
    - 入参最少要求：`customer_name`、`item_name_cn`；
    - 自动解析/创建客户（复用客户自动创建逻辑）；
    - 自动分配默认仓库与唯一 `inbound_no`（前缀 `MAN-`）；
    - 支持写入单条货物常用字段（日期、店铺、位置、箱数、数量、金额、CBM、长宽高、备注等）。
  - 前端“手动导入”页新增“手动导入单条数据”表单：
    - 支持直接录入一条货物并提交；
    - 提交后刷新客户、在库列表与总览，并弹窗展示新货物 ID 与单号。
- 更新原因：满足“无需 Excel、可快速补录单条货物”的现场操作需求。

### 76. 撤销组柜改为回退到 DRAFT（不销毁货柜）
- 时间：2026-04-09
- 更新内容：
  - 调整 `revoke_container` 行为（`app/services/containers.py`）：
    - 货柜状态由 `CONFIRMED` 回退到 `DRAFT`（不再改为 `REVOKED`）；
    - 货物状态由 `SHIPPED` 回退为 `ALLOCATED`；
    - 保留 `container_items` 绑定关系，不再清空柜内货物；
    - 清理 `confirmed_at/revoked_at`，保留可继续编辑组柜。
  - 新增保护：
    - 若该柜已有 `DRAFT/POSTED` 结算单，禁止撤销组柜，需先撤销结算流程。
  - 前端提示文案同步：
    - 撤销后提示为“回到 DRAFT，货物保持在柜内待编辑”。
- 更新原因：满足“撤销组柜是回退状态而非销毁货柜”的业务定义，避免撤销时丢失装柜明细。

### 77. 修复货物客户名修改未生效（编辑只保留客户名）
- 时间：2026-04-09
- 更新内容：
  - 调整“手动修改在库商品”表单：
    - 移除 `客户ID` 输入项，避免旧 ID 优先导致客户名修改被覆盖。
  - 调整编辑回填逻辑：
    - 点击“编辑”时不再回填客户ID，仅回填客户名。
  - 调整提交逻辑：
    - 客户字段仅使用 `customer_name` 解析得到 `customer_id` 后更新。
- 更新原因：修复“提示修改成功但实际客户未变更”的问题，消除客户ID与客户名同时存在时的冲突。

### 78. 确认组柜前强制校验柜主
- 时间：2026-04-09
- 更新内容：
  - 调整 `confirm_container`（`app/services/containers.py`）：
    - 从 `DRAFT -> CONFIRMED` 前，强制要求 `master_customer_id` 已设置；
    - 并校验该柜主客户在当前柜内确有货物。
  - 若不满足，直接报错并阻止确认出柜。
- 更新原因：满足“确认组柜前必须设置柜主”的业务约束，避免后续结算与同步出现主单客户缺失。
