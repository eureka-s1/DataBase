# Update Log

## 2026-04-04

### 1. 新增数据库设计文档
- 时间：2026-04-04
- 更新内容：创建 `agent/database_design.md`，完成需求整合、原始数据采样、完整数据库设计、技术栈、开发流程、可扩展性分析。
- 更新原因：落实 `agent/AGENTS.md` 指定的设计产出要求。

### 2. 设计文档补充（别名 + 环境）
- 时间：2026-04-04
- 更新内容：
  - 新增“客户别名映射”需求与表结构 `customer_aliases`。
  - 新增“WSL 开发 -> Windows 运行”环境约束与发布要求。
  - 在建库 SQL 与示例 SQL 中加入别名映射相关语句。
- 更新原因：响应最新业务要求，降低历史多命名导致的客户识别错误，并确保跨环境可部署。

### 3. 第一阶段代码实现（MVP 基础）
- 时间：2026-04-04
- 更新内容：
  - 新增数据库脚本：`schema/schema.sql`。
  - 新增应用代码：`app/`（配置、数据库连接、客户与别名服务、Flask 接口）。
  - 新增脚本：`scripts/init_db.py`、`scripts/import_alias_map.py`。
  - 新增跨环境文件：`.env.example`、`start_windows.bat`、`requirements.txt`。
  - 更新 `README.md` 说明启动方式和接口。
- 更新原因：进入开发实施阶段，先建立可运行基线并打通客户别名能力。

### 4. 第二阶段实现（覆盖核心业务需求）
- 时间：2026-04-04
- 更新内容：
  - 新增认证与账户能力：默认管理员、登录、改密、退出会话。
  - 完成入库业务：入库增删改查、体积自动计算、在库查询。
  - 完成拼柜业务：建柜、装柜容量校验、确认出柜、撤回恢复。
  - 完成结算业务：定金流水、按客户体积分摊运费、自动抵扣、账单过账。
  - 完成报表导出：日入库、库存、总账、账单（Excel/PDF）。
  - 完成备份能力：SQLite 备份接口与脚本，写入备份日志。
  - 完成 Excel 导入链路：模板识别、字段映射、预览与执行接口，支持 dry-run。
  - 新增基础 Web 页面：登录页、首页、业务面板。
  - 新增端到端冒烟脚本 `scripts/smoke_test.py`，覆盖登录→入库→拼柜→结算→导出→备份→导入预演。
- 更新原因：根据“直接做到完成所有需求，停在导入数据前”为目标，交付可运行版本并将真实数据导入保留为下一步人工确认操作。

### 5. 新增项目打包分发脚本（Windows/跨平台）
- 时间：2026-04-04
- 更新内容：
  - 新增打包脚本：`scripts/package_release.py`（生成 `dist/*.zip` 与 `*.sha256`）。
  - 新增 Windows 一键打包入口：`package_release.bat`。
  - 更新 `README.md`：新增“非技术用户一键启动”和“一键打包（发给同事）”说明。
- 更新原因：支持在 Windows 测试前快速生成发布包，并便于直接分发给业务同事使用。

### 6. 打包引导文案本地化（中文）
- 时间：2026-04-04
- 更新内容：
  - 将发布包内 `WINDOWS_QUICK_START.txt` 改为中文说明。
  - 强化“首次启动等待、不要关闭命令窗口、默认数据目录、常见问题”等引导细节。
  - 使用 `UTF-8-SIG` 写入，提升 Windows 记事本兼容性，减少乱码风险。
- 更新原因：提升非技术用户的可读性与可操作性，降低分发后使用门槛。

### 7. Web 面板重构与功能补齐
- 时间：2026-04-04
- 更新内容：
  - 重构 `app/templates/dashboard.html`，改为“模块切换式”布局，避免全部表格并排挤在同一页。
  - 新增网页可操作能力：修改密码、在库查询、总账查询、客户创建、别名维护（折叠显示）、价格规则维护。
  - 新增拼柜操作表单：建柜、装柜、移除、占用查询、确认、撤回。
  - 新增结算操作表单：登记定金、生成结算单、结算单过账。
  - 新增导入导出与备份表单：导入预览/执行、日入库/库存/总账/账单导出、数据库备份。
  - 新增后端接口 `GET /settlements` 用于前端查询结算单列表。
- 更新原因：响应“网页端功能缺失、查询能力不足、信息过于拥挤”的反馈，提升可用性与操作闭环。

### 8. 导入测试 Loop 与导入器迭代
- 时间：2026-04-04
- 更新内容：
  - 新增导入规范文档：`agent/import_data.md`。
  - 新增导入执行模板：`agent/import_run_template.md`。
  - 新增导入体检与报告：`agent/import_loop_report.md`。
  - 新增脚本：`scripts/bootstrap_customers_from_data.py`（按 `2025data` 目录预置客户与别名）。
  - 新增脚本：`scripts/run_import_loop.py`（批量执行 `preview -> dry-run -> commit` 并生成报告）。
  - 迭代 `app/services/importer.py`：
    - 扩展真实模板表头映射（含 `ITEM NAME/CNS/DEPOZIT` 等变体）。
    - 支持无表头“裸数据”模板解析与文件夹客户名回退。
    - 修复 `import_batches.batch_no` 唯一键冲突（批次号增加随机后缀）。
    - 修复 `inbound_no` 跨文件重复（单号引入 `batch_id` 维度）。
    - 增加非货物行过滤（如 `CBM FREIGHT/SEND TO ME/100CTNS` 等）。
  - 更新原因：支撑“清库后立即进入导入+测试循环”，先确保样本批量可导，再逐步扩大导入规模。

### 9. 导入 Loop 扩大到全量候选文件
- 时间：2026-04-04
- 更新内容：
  - 执行第 3 轮：`limit=600, commit-files=60`，达成 `parse_fail=0`、`dry_run failed_rows=0`。
  - 执行第 4 轮（全量）：`limit=0, commit-files=100`。
  - 全量结果：候选文件 `612`，解析成功 `611`，解析失败 `1`，`dry-run failed_rows=0`，正式导入 `100` 个文件。
  - 修复多级目录客户回退策略：默认客户名优先取 `2025data` 下一级目录，避免 `2025 11/2025 12` 等子目录名误映射。
- 更新原因：将导入能力从小批样本推进到全量候选验证，并识别剩余极少量异常文件供后续专项处理。

### 10. 全量 612/612 导入测试闭环
- 时间：2026-04-04
- 更新内容：
  - 针对专项失败文件 `2025data/胡订货/2-18 EKENE .1.xlsx` 追加订货单模板兼容：
    - 增加表头映射 `DESCRIPTION/DESCRPETION`。
    - 表头检测允许 `item_no` 作为可导入主字段（无 `item_name_cn` 时回退）。
  - 复跑全量 loop（`limit=0, commit-files=100`）结果：
    - 候选文件 `612`，解析成功 `612`，解析失败 `0`。
    - `dry_run`：总行数 `29801`，成功 `29801`，失败 `0`。
    - 正式导入文件数 `100`。
- 更新原因：完成全量导入测试主目标，形成可持续复用的导入验证闭环。
